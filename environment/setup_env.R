# Baseline environment setup helper (extend as needed)
# Goal: make it easy for R users to configure a Python env via reticulate.
# Example pattern:
#   reticulate::use_condaenv("fueldeep3d", required = TRUE)